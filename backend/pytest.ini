[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: mark test as requiring the full Docker Compose stack to be running
                 (db, redis, backend, worker, frontend).
                 Run with: pytest tests/ -v -m integration
